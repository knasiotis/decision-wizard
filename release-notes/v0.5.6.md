<!-- One bullet per change, plainly worded, with its commit id in brackets.
     Add a "## Features" section above the fixes when there is one. -->

## Fixes

- An edge that crosses a layer is drawn with right angles, so it no longer runs behind the node in between (d326a69)
- Tapping a stub chip keeps the canvas on screen instead of shoving it to one side (22a0b8c)
- The node a jump lands on no longer stays stuck part way through its flash (e0c51d4)
