<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      No permissions, deliberately. The app is entirely offline: no network, no
      inference, no telemetry. If a permission ever appears here, something has
      gone wrong with the design.
    -->

    <application
        android:name=".DecisionWizardApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.DecisionWizard">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:label="@string/app_name"
            android:theme="@style/Theme.DecisionWizard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              Tapping a .dwiz file opens the app. The pattern is repeated because
              Android's pathPattern matcher stops matching once the filename
              contains extra dots, and a graph called "Router v1.2" is enough to
              trigger it. Each extra line covers one more dot.

              mimeType is */* because .dwiz has no registered type; the
              pathPattern is what actually narrows this filter.
            -->
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:host="*" />
                <data android:mimeType="*/*" />
                <data android:pathPattern=".*\\.dwiz" />
                <data android:pathPattern=".*\\..*\\.dwiz" />
                <data android:pathPattern=".*\\..*\\..*\\.dwiz" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\.dwiz" />
            </intent-filter>
        </activity>
    </application>

</manifest>
