<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      The platform theme only covers the window before Compose draws. Everything
      visible is Material 3 from Theme.kt. DayNight is used so a dark-mode launch
      does not flash white.
    -->
    <style name="Theme.DecisionWizard" parent="@android:style/Theme.DeviceDefault.DayNight">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
